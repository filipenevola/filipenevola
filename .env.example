# MongoDB connection (database: palmeirasapp, collection: posts)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/?retryWrites=true&w=majority

# Newsletter (optional — still uses Lemeno if configured)
LEMENO_API_KEY=
LEMENO_APP_URL=
